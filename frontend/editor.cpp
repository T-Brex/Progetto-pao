#ifndef EDITOR_H
#define EDITOR_H


#endif

/*
double divisionSpacing = 25 * zoom;
int lineCounter = 1;

//linee di divisione sull'asse x
for (int x = center.x() + divisionSpacing; x < width(); x += divisionSpacing) {
    if(zoom>0.75){
        painter.drawLine(x, center.y() - 2, x, center.y() + 2);
        painter.drawText(x - 10, center.y() + 20, QString::number(round((x - center.x()) / round(divisionSpacing))));
    }else{
        if (zoom>0.45){
            if (lineCounter % 2 == 0) {
                painter.drawLine(x, center.y() - 2, x, center.y() + 2);
                painter.drawText(x - 10, center.y() + 20, QString::number(round((x - center.x()) / round(divisionSpacing))));
            }
        }else{
            if (lineCounter % 3 == 0) {
                painter.drawLine(x, center.y() - 2, x, center.y() + 2);
                painter.drawText(x - 10, center.y() + 20, QString::number(round((x - center.x()) / round(divisionSpacing))));
            }
        }

    }

    lineCounter++;
}

lineCounter = 1;//----------------------------------------------

for (int x = center.x() - divisionSpacing; x > 0; x -= divisionSpacing) {
    if(zoom>0.75){
        painter.drawLine(x, center.y() - 2, x, center.y() + 2);
        painter.drawText(x - 10, center.y() + 20, QString::number(round((x - center.x()) / round(divisionSpacing))));
    }else{
        if (zoom>0.45){
            if (lineCounter % 2 == 0) {
                painter.drawLine(x, center.y() - 2, x, center.y() + 2);
                painter.drawText(x - 10, center.y() + 20, QString::number(round((x - center.x()) / round(divisionSpacing))));
            }
        }else{
            if (lineCounter % 3 == 0) {
                painter.drawLine(x, center.y() - 2, x, center.y() + 2);
                painter.drawText(x - 10, center.y() + 20, QString::number(round((x - center.x()) / round(divisionSpacing))));
            }
        }

    }



    lineCounter++;
}

lineCounter = 1;//----------------------------------------------

//linee di divisione sull'asse y
for (int y = center.y() + divisionSpacing; y < height(); y += divisionSpacing) {
    if(zoom>0.75){
        painter.drawLine(center.x() - 2, y, center.x() + 2, y);
        painter.drawText(center.x() + 10, y + 5, QString::number(round((center.y() - y) / round(divisionSpacing))));
    }else{
        if (zoom>0.45){
            if (lineCounter % 2 == 0) {
                painter.drawLine(center.x() - 2, y, center.x() + 2, y);
                painter.drawText(center.x() + 10, y + 5, QString::number(round((center.y() - y) / round(divisionSpacing))));
            }
        }else{
            if (lineCounter % 3 == 0) {
                painter.drawLine(center.x() - 2, y, center.x() + 2, y);
                painter.drawText(center.x() + 10, y + 5, QString::number(round((center.y() - y) / round(divisionSpacing))));
            }
        }

    }



    lineCounter++;
}

lineCounter = 1;//----------------------------------------------

for (int y = center.y() - divisionSpacing; y > 0; y -= divisionSpacing) {
    if(zoom>0.75){
        painter.drawLine(center.x() - 2, y, center.x() + 2, y);
        painter.drawText(center.x() + 10, y + 5, QString::number(round((center.y() - y) / round(divisionSpacing))));
    }else{
        if (zoom>0.45){
            if (lineCounter % 2 == 0) {
                painter.drawLine(center.x() - 2, y, center.x() + 2, y);
                painter.drawText(center.x() + 10, y + 5, QString::number(round((center.y() - y) / round(divisionSpacing))));
            }
        }else{
            if (lineCounter % 3 == 0) {
                painter.drawLine(center.x() - 2, y, center.x() + 2, y);
                painter.drawText(center.x() + 10, y + 5, QString::number(round((center.y() - y) / round(divisionSpacing))));
            }
        }

    }



    lineCounter++;
}
*/
